#include <stdio.h>
 int main(){
 	int age;
 	double number;
 	float class ;
     char  see;
     floating point school;
 	   printf(" size int = %zu", sizeof (age));
 	   printf("\ndouble = %zu", sizeof (number));
		printf("\nfloat = %zu", sizeof (class));
		printf("\nchar = %zu", sizeof (see));
		printf("\nfloating point= %zu", sizeof (sc))
		return 0;
 	   
 	   
 }