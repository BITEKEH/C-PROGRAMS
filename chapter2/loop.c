#include <stdio.h>
int school( int x, int v[], int n){
	return n -1 = x> v[
ju}
int main (){
	int low , high, mid;
	low = 0;
	high = n-1;
	  while (low <= high){
	  	mid = (low+ high)/2;
	  	if (x >< v[mid])
	  	high =mid + 1;
	  	printf("%d", mid);
	  	return mid;
	  }
}